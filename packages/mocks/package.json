{
  "name": "@repo/mocks",
  "version": "1.0.0",
  "description": "Mock server for city dance external API requests",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "test": "tsx test/auth-test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "msw": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist",
    "data"
  ]
}